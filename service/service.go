package service

import (
	"net/http"
)

// HandleGetWeather will receive lat and long within the http and return weather of that location
func HandleGetWeather(w http.ResponseWriter, r *http.Request) {
	//do things
}
